spring:
  application:
    name: Service films
  datasource:
    username: sa
    password:
  h2:
    console:
      enabled: true

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: '*'
