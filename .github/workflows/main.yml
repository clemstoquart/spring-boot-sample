name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2.3.2
    
    - uses: actions/setup-java@v1
      with:
        java-version: '11'
        
    - run: java --version
      
    - name: Build
      run: ./gradlew build
